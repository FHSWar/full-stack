{
  "name": "ui-auto-monorepo",
  "author": "fhswar",
  "email": "fhswar0504@163.com",
  "private": true,
  "version": "1.0.0",
  "description": "ui-auto",
  "keywords": [
    "koa",
    "vue3",
    "typescript"
  ],
  "license": "ISC",
  "workspaces": [
    "client",
    "server-chief",
    "server-worker",
    "shared"
  ],
  "engines": {
    "node": ">=16.14.2"
  },
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:client": "cd client && npm run dev",
    "dev:server-chief": "cd server-chief && npm run dev",
    "dev:server-worker": "cd server-worker && npm run dev",
    "lint": "npm-run-all --parallel lint:*",
    "lint:chief": "cd server-chief && npm run lint",
    "lint:worker": "cd server-worker && npm run lint",
    "lint:shared": "cd shared && npm run lint",
    "prepare": "husky install"
  },
  "dependencies": {
    "deasync": "^0.1.26"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "npm-run-all": "^4.1.5"
  },
  "lint-staged": {
    "server-chief": [
      "eslint . --fix", "git add"
    ],
    "server-worker": [
      "eslint . --fix", "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
